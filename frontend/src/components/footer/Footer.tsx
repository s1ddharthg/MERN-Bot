const Footer = () => {
  return (
    <footer
      style={{
        width: "100%",
        padding: "2rem 0",
        marginTop: "3rem",
        borderTop: "3px solid var(--border)",
        textAlign: "center",
        color: "var(--text-dim)",
        fontSize: "14px",
      }}
    >
      Chatbox — your own AI chatbot
    </footer>
  );
};

export default Footer;
